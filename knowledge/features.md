# Features

Claude and Claude Code features, capabilities, and model updates.

- **Claude Opus 4.6 launched** — Released February 5, 2026 with 1M token context window, stronger coding, better planning/debugging, and improved financial analysis. (Source: https://its.northeastern.edu/2026/02/26/anthropics-latest-claude-models-are-here-and-theyre-a-big-leap-forward/)
- **Claude Sonnet 4.6 released** — Launched February 17, 2026 with near-Opus performance on coding and document comprehension, plus improved computer use. (Source: https://releasebot.io/updates/anthropic/claude)
- **1M context window generally available** — Available on Max, Team, and Enterprise plans for Opus 4.6 and Sonnet 4.6 at standard pricing. No beta header required. (Source: https://code.claude.com/docs/en/changelog)
- **Opus 4.6 output tokens increased to 64k** — Default max output tokens raised to 64k, upper bound to 128k for both Opus 4.6 and Sonnet 4.6 (v2.1.77). (Source: https://github.com/anthropics/claude-code/releases)
- **Voice mode in Claude Code** — Push-to-talk via spacebar, customisable keybinding (voice:pushToTalk in keybindings.json), rolling out progressively. (Source: https://pasqualepillitteri.it/en/news/381/claude-code-march-2026-updates)
- **MCP Elicitation support** — MCP servers can now request structured input mid-task via interactive dialog. New Elicitation and ElicitationResult hooks added (v2.1.76). (Source: https://code.claude.com/docs/en/changelog)
- **Worktree sparse checkout** — New worktree.sparsePaths setting for large monorepos to check out only needed directories (v2.1.76). (Source: https://github.com/anthropics/claude-code/releases)
- **Context command improvements** — /context now gives actionable suggestions identifying context-heavy tools, memory bloat, and optimisation tips (v2.1.74). (Source: https://code.claude.com/docs/en/changelog)
- **Custom auto-memory directory** — New autoMemoryDirectory setting to choose where auto-memory files are stored (v2.1.74). (Source: https://code.claude.com/docs/en/changelog)
- **Model overrides setting** — New modelOverrides setting to map model picker entries to custom provider model IDs (v2.1.73). (Source: https://code.claude.com/docs/en/changelog)
- **Fast mode for Opus 4.6** — Research preview providing up to 2.5x faster output token generation via the `speed` parameter at premium pricing. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Compaction API** — Server-side context summarization in beta, enabling effectively infinite conversations on Opus 4.6. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Extended thinking display control** — New `thinking.display: "omitted"` option omits thinking content from responses for faster streaming while preserving signatures for multi-turn continuity. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Automatic prompt caching** — Single `cache_control` field automatically caches the last cacheable block and moves the cache point forward as conversations grow. No manual breakpoint management required. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **600 media items per 1M context request** — Media limit raised from 100 to 600 images or PDF pages per request when using the 1M token context window. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Plugin data persistence** — New `${CLAUDE_PLUGIN_DATA}` variable provides persistent storage for plugin state across sessions (v2.1.78). (Source: https://github.com/anthropics/claude-code/releases)
- **StopFailure hook event** — New hook event fires on API errors like rate limits and auth failures, enabling automated error handling (v2.1.78). (Source: https://github.com/anthropics/claude-code/releases)
- **Data residency controls** — New `inference_geo` parameter lets you specify where model inference runs. US-only inference available at 1.1x pricing for models released after February 2026. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Claude Opus 4.7 launched** — Released April 13, 2026 as the new default on Max and Team Premium. Introduced `xhigh` effort level as the recommended setting for most coding work, with an interactive `/effort` slider. (Source: https://code.claude.com/docs/en/whats-new)
- **Auto mode** — A classifier-based permission system that handles safe actions without interruption and blocks risky ones. Research preview in March 2026, now available on Pro, Bedrock, Vertex, and Foundry for Opus 4.7 and 4.8 (v2.1.83+). (Source: https://code.claude.com/docs/en/whats-new)
- **Computer use in CLI** — Claude can open native apps, click through UI, and verify changes from the terminal. Research preview launched in v2.1.86 (Week 14, March–April 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **Claude Managed Agents** — Sandboxed agent platform with checkpointing and credential scoping, enabling production agents to run in environments you control with MCP access. Announced at Code with Claude 2026. (Source: https://www.infoq.com/news/2026/05/code-with-claude/)
- **Claude Opus 4.8 launched** — Released May 28, 2026. Benchmarks: agentic coding 64.3%→69.2%, reasoning with tools 54.7%→57.9%, computer use 82.8%→83.4%. High-effort defaults, fast mode at 2× standard rate for 2.5× speed. Pricing unchanged from 4.7. (Source: https://9to5mac.com/2026/05/28/anthropic-upgrades-claude-with-new-opus-4-8-model-heres-whats-new/)
- **Dynamic workflows** — Orchestrate dozens to hundreds of subagents from a Claude-written script; trigger with the keyword "ultracode" (violet highlighting). Research preview in v2.1.154. (Source: https://code.claude.com/docs/en/whats-new)
- **Plugin auto-loading from .claude/skills** — Plugins in `.claude/skills` directories load automatically without marketplace requirement. `claude plugin init <name>` scaffolds new plugins with autocomplete (v2.1.157). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **MessageDisplay hook** — New hook event that fires on each message, enabling message transformation before display (v2.1.152). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Messages API mid-conversation system entries** — Messages API now accepts system entries mid-conversation without disrupting prompt caching, enabling dynamic context injection. (Source: https://9to5mac.com/2026/05/28/anthropic-upgrades-claude-with-new-opus-4-8-model-heres-whats-new/)
- **Self-hosted sandboxes for Managed Agents** — Tool execution now runs in your own infrastructure while orchestration stays on Anthropic's servers, keeping files within your security perimeter. Available as public beta on Claude Platform on AWS (announced May 2026). (Source: https://claude.com/blog/code-w-claude-london-2026-rethinking-how-we-build)
- **Security-guidance plugin** — Automatically reviews Claude's changes for security vulnerabilities as it works, surfacing issues inline without a separate review step. Bundled in Week 22 (v2.1.150–v2.1.157). (Source: https://code.claude.com/docs/en/whats-new)
- **Safety warning before writing shell startup files** — Claude now prompts for confirmation before writing to shell startup files (e.g. `.bashrc`, `.zshrc`) or build-tool config files, preventing accidental environment changes (v2.1.160). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Fast mode available for Claude Opus 4.7** — Fast mode (`speed: "fast"`) now supports claude-opus-4-7, providing significantly faster output token generation at premium pricing (Week 20, v2.1.139). (Source: https://code.claude.com/docs/en/whats-new)
- **Parallel tool calls now independent** — In agentic workflows, a failure in one parallel tool call no longer cancels the other calls in the batch; each succeeds or fails independently (v2.1.161). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Slash commands fill into prompt bar before executing** — Selecting a slash command now inserts it into the prompt bar for review and editing before submission, improving discoverability and preventing accidental execution (v2.1.162). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **`fallbackModel` setting** — Configure up to three backup models tried in order when the primary model is overloaded or unavailable; `--fallback-model` flag also applies to interactive sessions (v2.1.166). (Source: https://github.com/anthropics/claude-code/releases)
- **Thinking fully disabled on default-thinking models** — `MAX_THINKING_TOKENS=0`, `--thinking disabled`, and the per-model thinking toggle now disable thinking computation (not just display) on models that think by default, reducing latency and cost when extended reasoning is unneeded (v2.1.166). (Source: https://github.com/anthropics/claude-code/releases)
- **Edit no longer requires Read after single-file grep** — After running `grep`/`egrep`/`fgrep` on a single file, Claude Code no longer requires a separate Read call before issuing an Edit; the grep result satisfies the read precondition (v2.1.160). (Source: https://github.com/anthropics/claude-code/releases)
- **`--tools` flag unlocks dedicated Grep/Glob tools** — Explicitly listing `Grep` or `Glob` in the `--tools` flag provides dedicated search tools rather than relying on Bash, improving precision and permission scoping for search-heavy pipelines (v2.1.162). (Source: https://github.com/anthropics/claude-code/releases)
- **Opus 4.8 effort defaults to `high` across all surfaces** — On Claude Opus 4.8, the effort parameter defaults to `high` across all surfaces including Claude Code and the Messages API, enabling deeper reasoning by default without explicit configuration. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Adaptive thinking on Opus 4.8 triggers only when needed** — With adaptive thinking enabled, Opus 4.8 skips extended reasoning on turns that don't require it, reducing wasted thinking tokens compared to Opus 4.7 at the same effort level. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Prompt cache minimum lowered to 1,024 tokens on Opus 4.8** — The minimum cacheable prompt length for prompt caching dropped from the Opus 4.7 threshold to 1,024 tokens on Opus 4.8, making caching accessible for shorter prompts. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Mobile push notifications in Claude Code** — Claude Code pings your phone when a long-running task completes or when Claude is blocked waiting for your input, surfacing session state without keeping the terminal open. Launched Week 16 (April 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **Session recap after terminal inactivity** — When you return to an unfocused terminal, Claude Code shows a recap of everything that happened while you were away, so you can quickly re-orient without scrolling history. Launched Week 17 (April 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **`--safe-mode` flag and `CLAUDE_CODE_SAFE_MODE` env var** — Disables all customisations (CLAUDE.md, plugins, skills, hooks, MCP servers) for a clean troubleshooting environment without a full reinstall (v2.1.169). (Source: https://code.claude.com/docs/en/changelog)
- **`disableBundledSkills` setting and `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS` env var** — Hides all built-in bundled skills, workflows, and slash commands, useful for organisations that want only their own skills visible (v2.1.169). (Source: https://code.claude.com/docs/en/changelog)
- **CLAUDE.md "too long" warning scales with model context window** — The warning threshold now adjusts dynamically based on the active model's context size rather than a fixed line count (v2.1.169). (Source: https://code.claude.com/docs/en/changelog)
- **Claude Fable 5 launched** — Launched June 9, 2026 as Anthropic's most capable widely-released model. State-of-the-art on nearly all benchmarks across software engineering, knowledge work, vision, and scientific research. 1M token context window by default, 128k max output tokens, always-on adaptive thinking. Priced at $10/$50 per MTok. Model ID: `claude-fable-5`. (Source: https://www.anthropic.com/news/claude-fable-5-mythos-5)
- **Claude Mythos 5 launched** — Restricted-access companion to Fable 5, launched June 9, 2026 for Project Glasswing participants. Same underlying architecture with safeguards lifted in certain areas; described as having the strongest cybersecurity capabilities of any available model. Model ID: `claude-mythos-5`. Priced at $10/$50 per MTok. (Source: https://www.anthropic.com/news/claude-fable-5-mythos-5)
- **Fable 5 / Mythos 5 new tokenizer produces ~30% more tokens** — Claude Fable 5 and Mythos 5 use the tokenizer introduced with Opus 4.7; the same text produces roughly 30% more tokens compared to models released before Opus 4.7. Use the token counting API with `model: "claude-fable-5"` to measure prompts before migrating. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Claude Fable 5 requires 30-day data retention** — Fable 5 is not available under zero data retention; API requests must accept a 30-day retention policy. Only model with this requirement as of June 2026. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Workflow keyword trigger setting in /config** — New setting in `/config` prevents the word "workflow" in a prompt from unintentionally launching a dynamic workflow. Backspace pressed immediately after a workflow trigger now dismisses the workflow request (same as alt+w) instead of deleting a character (v2.1.169). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Sub-agents can spawn nested sub-agents up to 5 levels deep** — In v2.1.172 (June 10, 2026), Claude Code sub-agents gained the ability to recursively spawn their own sub-agents, up to 5 levels of nesting. Enables more sophisticated multi-agent hierarchies without manual orchestration. (Source: https://github.com/anthropics/claude-code/releases)
- **Fable 5 / Mythos 5: adaptive thinking only, `thinking: disabled` not supported** — On Claude Fable 5 and Claude Mythos 5, adaptive thinking is the sole thinking mode. Setting `thinking: {"type": "disabled"}` returns a 400 error, as do manual extended thinking budgets and assistant prefill. Pass thinking blocks back unchanged in multi-turn conversations; set `display: "summarized"` to receive readable thinking summaries. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **`enforceAvailableModels` managed setting** — When enabled, the `availableModels` allowlist also constrains the Default model (a Default that would resolve to a disallowed model falls back to the first allowed model), and user or project settings can no longer widen a managed `availableModels` list. Useful for organisations enforcing model governance across workspaces (v2.1.175, June 12, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **`wheelScrollAccelerationEnabled` setting** — New setting to disable mouse-wheel scroll acceleration in fullscreen mode; useful if you find the acceleration jarring in the TUI renderer (v2.1.174). (Source: https://github.com/anthropics/claude-code/releases)
- **Session titles in conversation language** — Session titles are now generated in the language of your conversation. Set the `language` setting to pin a specific language (v2.1.176). (Source: https://github.com/anthropics/claude-code/releases)
- **`footerLinksRegexes` setting** — Add regex-matched link badges to the footer row; configurable in user or managed settings for quick navigation to related docs or dashboards (v2.1.176). (Source: https://github.com/anthropics/claude-code/releases)
- **Nested `.claude/skills` directories auto-load** — Skills inside nested `.claude/skills` directories now load automatically when working on files in that directory. On name clash, the nested skill appears as `<dir>:<name>` to disambiguate from the parent-level skill (v2.1.178). (Source: https://code.claude.com/docs/en/changelog)
- **Nearest `.claude/` directory wins on name collision** — For agents, workflows, and output styles, the `.claude/` directory closest to the current working directory takes precedence on name collision; project-scope workflow saves target the closest existing `.claude/workflows/` (v2.1.178). (Source: https://code.claude.com/docs/en/changelog)
- **Auto Mode pre-launch classifier for subagents** — Auto Mode now evaluates each subagent spawn through the classifier before the subagent launches, catching risky delegations earlier in the pipeline rather than only after the child agent starts running (v2.1.178). (Source: https://code.claude.com/docs/en/changelog)
- **Workflow keyword requires explicit phrase to trigger** — The dynamic workflow trigger now fires only on explicit phrases like "run a workflow" or "workflow:" rather than the keyword alone, reducing accidental workflow launches while composing prompts. Highlight colour changed from violet to purple shimmer (v2.1.178). (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.179 stability release** — Released June 16, 2026. Key fixes: partial responses now preserved on mid-stream connection drops instead of showing raw errors; WSL2 mouse-wheel scroll regression (introduced in v2.1.172) fixed in Windows Terminal and VS Code; Linux sandbox `denyRead`/`allowRead` glob patterns over large directory trees no longer cause enormous Bash tool descriptions and unusable sessions; Ctrl+O now correctly shows subagent transcripts; remote session background tasks no longer appear stuck between turns. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Dreaming for Managed Agents** — Asynchronous process that reviews past agent session transcripts alongside a memory store, merges duplicates, replaces stale entries, and surfaces new insights into a reorganised memory store. Accepts 1–100 past sessions per run; can update memory automatically or hold changes pending your review. Launched at Code with Claude 2026 (May 6) as a research preview. Harvey legal reported a ~6× jump in agent task-completion rates after enabling Dreaming for drafting workflows. (Source: https://platform.claude.com/docs/en/managed-agents/dreams)
- **Claude Code v2.1.181 release** — Released June 17, 2026. New features: `/config key=value` syntax sets settings from the prompt bar; `sandbox.allowAppleEvents` enables macOS Apple Events in sandboxed commands; `CLAUDE_CLIENT_PRESENCE_FILE` suppresses mobile push notifications while at the machine. Improvements: idle subagents auto-hide after 30s in the subagent panel; MCP OAuth browser page auto-closes on success; fullscreen URL opening now requires Cmd+click (macOS) / Ctrl+click. (Source: https://github.com/anthropics/claude-code/releases)
- **Auto mode safety guardrails for destructive commands** — In v2.1.183 (June 19, 2026), auto mode blocks destructive git commands when you didn't ask to discard work (`git reset --hard`, `git checkout -- .`, `git clean -fd`, `git stash drop`) and blocks `git commit --amend` when the commit was not made by the agent in the current session. Infrastructure-destroying commands (`terraform destroy`, `pulumi destroy`, `cdk destroy`) are also blocked unless explicitly requested. (Source: https://code.claude.com/docs/en/changelog)
- **Stream-stall detection threshold extended to 20s** — In v2.1.185 (June 20, 2026), the hint that appears during API silence changed from "No response from API · Retrying in …" to "Waiting for API response · will retry in …", and the silence threshold before the hint appears was extended from 10s to 20s, reducing false-alarm interruptions on slow responses. (Source: https://code.claude.com/docs/en/changelog)
- **Model deprecation warnings in headless mode** — Claude Code v2.1.183 emits a warning on stderr in print mode (`-p`) and in agent frontmatter when the requested model is deprecated or has been automatically updated to a newer version, making model-migration issues visible in CI pipelines and scripts. (Source: https://code.claude.com/docs/en/changelog)
- **`attribution.sessionUrl` setting** — New setting that controls whether the claude.ai session link is included in commits and PRs. Useful in web and Remote Control sessions where the session URL is ephemeral and the link would be meaningless in the git log (v2.1.183). (Source: https://code.claude.com/docs/en/changelog)
- **`!` bash commands trigger automatic Claude responses** — In v2.1.186 (June 22, 2026), running a `! <command>` shell command automatically feeds the output back to Claude as a follow-up. Disable with `"respondToBashCommands": false` in settings if you prefer manual control. (Source: https://github.com/anthropics/claude-code/releases)
- **Safer automatic edits in `acceptEdits` mode** — Claude Code now prompts for confirmation before writing files that can execute code (scripts, Makefiles, CI configs) when running in `acceptEdits` mode, preventing silent execution-ready changes (Week 23, v2.1.158–v2.1.165). (Source: https://code.claude.com/docs/en/whats-new)
- **Managed deployment version requirements** — Managed Claude Code deployments can now declare an approved version range (`requiredMinVersion` / `requiredMaxVersion`) so enterprise admins can pin users to validated releases and prevent unreviewed client updates (Week 23, v2.1.158–v2.1.165). (Source: https://code.claude.com/docs/en/whats-new)
- **Skills section in `/plugin` Installed tab** — The `/plugin` Installed tab now surfaces a dedicated Skills section, making it easy to see which skills are loaded alongside full plugins without listing them separately (v2.1.186). (Source: https://github.com/anthropics/claude-code/releases)
- **Artifacts in Claude Code (beta)** — Sessions can generate shareable, live-updating web pages (incident reports, PR walkthroughs, dashboards) directly from a Claude Code session. Content is private to the organisation with version history and access controls. Announced June 2026. (Source: https://releasebot.io/updates/anthropic)
- **Claude Code v2.1.187** — Released June 24, 2026. New `sandbox.credentials` setting blocks sandboxed commands from reading credential files; org-configured model restrictions surface in the model picker with a "restricted by your organisation's settings" message; `/btw` gains arrow-key navigation to review previous answers; `/plugin` redesigned to surface unused plugins for cleanup; remote MCP tool calls now auto-abort after 5 minutes instead of hanging indefinitely; structured output tool calls can no longer loop infinitely after a successful call; `--resume` fixed for "No conversation found" errors; Korean/CJK text corruption in terminal paste events resolved. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Fable 5 model ID `[1m]` suffix auto-stripped** — Claude Code v2.1.173 (June 24, 2026) automatically normalises Fable 5 model names by stripping the `[1m]` suffix before sending requests to the API, resolving 400 errors that appeared when the suffix was passed verbatim. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Streaming paragraphs render line-by-line** — Long paragraphs now stream to the terminal line-by-line instead of waiting for the first newline before displaying, reducing perceived latency on verbose responses (v2.1.191, June 24, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **~37% CPU reduction during streaming** — CPU usage during streaming responses reduced by approximately 37% through text-update coalescing at 100 ms intervals, improving performance on long outputs (v2.1.191, June 24, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Auto-retry for API connection drops mid-thinking** — Claude Code now automatically retries when the connection drops while Claude is in an extended thinking phase, instead of surfacing a "Connection closed while thinking" error (v2.1.191, June 24, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Trusted Devices for Remote Control Admins** — Team and Enterprise plan admins can now require device verification before team members can view or steer local Claude Code sessions remotely, preventing unauthorised access to live coding sessions (June 25, 2026). (Source: https://releasebot.io/updates/anthropic)
- **`autoMode.classifyAllShell` setting** — Routes all Bash/PowerShell commands through the auto-mode classifier rather than only arbitrary-code-execution patterns, giving auto mode broader visibility into shell activity (v2.1.193, June 25, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Live file path autocomplete in bash mode** — When using `!` to run shell commands, Claude Code now autocompletes file paths as you type (v2.1.193, June 25, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Automatic memory-pressure reaping for idle background shells** — Idle background shell commands are automatically reaped under memory pressure to prevent long-session memory bloat; disable with `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` (v2.1.193, June 25, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Background subagents surface permission prompts in main session** — Background subagents now relay their permission prompts to the main session interface instead of auto-denying them, letting unattended multi-agent runs continue when a permission is needed rather than silently failing (Week 26, v2.1.191–v2.1.193, June 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **`CLAUDE_CODE_DISABLE_MOUSE_CLICKS` env var** — Set `CLAUDE_CODE_DISABLE_MOUSE_CLICKS=1` to disable mouse-click interactions in fullscreen mode while keeping mouse-wheel scroll active; useful for terminal emulators that misinterpret click events in the TUI (v2.1.195, June 26, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Sonnet 5 launched** — Released June 30, 2026. Agentic coding benchmark: 63.2% (vs Opus 4.8's 69.2%, Sonnet 4.6's 58.1%); slightly outperforms Opus 4.8 on knowledge-work benchmarks. Promotional pricing: $2/$10 per MTok through August 31, then $3/$15. Default model for free and Pro plans; also the new default in Claude Code. Native 1M token context window. (Source: https://www.anthropic.com/news/claude-sonnet-5)
- **External plugins require explicit install consent** — Claude Code now requires user confirmation before installing external plugins that aren't already in your configuration, preventing silent third-party plugin installation (v2.1.195, June 26, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Code v2.1.196 release** — Released June 29, 2026. Adds organisation default models (admins configure in the org console; shows as "Org default" or "Role default" in `/model`), readable default session names, and clickable file attachments (Cmd/Ctrl-click reveals file in Finder/Explorer). Streaming idle watchdog is now on by default for all providers — aborts and retries after 5 minutes of silence; disable with `CLAUDE_ENABLE_STREAM_WATCHDOG=0`. Security: `claude mcp list`/`get` no longer spawns `.mcp.json` servers that a repo self-approved via a committed `.claude/settings.json`; untrusted workspaces show ⏸ Pending approval. `/code-review` token usage cut ~25% by merging five cleanup finders into one. Dozens of bug fixes: background session reliability across restarts/daemon restarts, PowerShell git/grep exit-code handling, MCP OAuth scope failures on enterprise IdPs, and `claude agents` session status accuracy. (Source: https://github.com/anthropics/claude-code/releases)
- **Fast mode removed from Opus 4.6, deprecated on Opus 4.7** — June 29, 2026: fast mode silently removed from `claude-opus-4-6`; requests with `speed: "fast"` now run at standard speed and standard rates without error. June 25, 2026: fast mode deprecated for `claude-opus-4-7` with removal on July 24, 2026 (after which `speed: "fast"` on 4.7 will return an error). Migrate to Claude Opus 4.8 to continue using fast mode. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Claude in Chrome generally available** — Claude in Chrome, which provides AI assistance directly in the browser, launched as generally available in Claude Code v2.1.198 (July 1, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Background agents auto-commit, push, and open draft PRs** — Background agents launched from `claude agents` now automatically commit, push, and open a draft PR when they finish code work in a worktree, instead of stopping to ask for input (v2.1.198, July 1, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Explore agent inherits main session's model** — The built-in Explore agent now runs on the same model as the main session (capped at Opus) rather than always running on Haiku, improving search quality for complex codebase tasks (v2.1.198, July 1, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Subagents and compaction inherit extended thinking config** — Subagents and context compaction now inherit the main session's extended thinking configuration, improving output quality on delegated and compacted tasks (v2.1.198, July 1, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Default permission mode renamed to "Manual"** — In Claude Code v2.1.200 (July 3, 2026), the "default" permission mode was renamed to "Manual" across the CLI, `--help`, VS Code, and JetBrains; `--permission-mode manual` and `"defaultMode": "manual"` are accepted alongside the legacy `"default"` value. AskUserQuestion dialogs now no longer auto-continue by default — opt into an idle timeout via `/config`. (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Sonnet 5 real-time cybersecurity safeguards** — Claude Sonnet 5 is the first Sonnet-tier model with real-time cybersecurity safety classifiers. Requests involving prohibited or high-risk cybersecurity topics may be refused mid-generation, returning `stop_reason: "refusal"` on an HTTP 200 response — not an error. Refused requests that produce no output are not billed. (Source: https://platform.claude.com/docs/en/about-claude/models/whats-new-sonnet-5)
- **Subagents run in background by default** — Subagents launched by Claude Code now run in the background by default, keeping the main session interactive while the subagent works. Subagents surface permission prompts to the main session interface rather than blocking or auto-denying silently (v2.1.198, July 1, 2026). (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Code v2.1.201: Sonnet 5 mid-conversation system role fix** — Claude Sonnet 5 sessions no longer use the mid-conversation system role for harness reminders, eliminating a source of unexpected context disruption in long Sonnet 5 sessions (v2.1.201, July 4, 2026). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Code v2.1.202: Dynamic workflow size setting** — Adds a "Dynamic workflow size" setting to control how many subagents Claude spawns in dynamic workflows; enhanced telemetry now tracks per-workflow agent counts. Also restores `/review` to single-pass flow, fixes crashes in history search, resolves Remote Control command failures, voice dictation issues, session resumption problems, and MCP error messaging (v2.1.202, July 7, 2026). (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Grey ⏸ badge for manual permission mode** — Claude Code v2.1.203 (July 7, 2026) adds a persistent grey ⏸ badge to the session footer whenever manual permission mode is active, giving a clear at-a-glance visual indicator of the current permission state alongside the existing Shift+Tab cycling shortcut. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Cowork expands to web and mobile** — Rolling out from July 7, 2026 (Max plan first, then other paid plans): Cowork sessions and files now sync across devices, background tasks continue running after closing the laptop, and Claude sends a mobile push notification when it reaches a decision only the user can make. Doubled usage limits extended through August 5, 2026. (Source: https://9to5mac.com/2026/07/07/anthropic-expanding-claude-cowork-to-mobile-and-web-details-here/)
- **Fable 5 redeployment includes new safety classifier blocking >99% of flagged jailbreak** — When Fable 5 returned on July 1, 2026, Anthropic deployed a new safety classifier that blocks the Amazon AWS-researchers' vulnerability (prompt Fable 5 to "read a codebase and fix software flaws") in over 99% of cases, rerouting blocked requests to Claude Opus 4.8. (Source: https://codersera.com/blog/when-is-claude-fable-5-coming-back-2026/)
- **Claude Code v2.1.205** — Released July 8, 2026. Auto mode now blocks tampering with session transcript files; `/doctor` upgraded to a full setup checkup that can diagnose and fix issues; auto-update downloads stream to disk, reducing peak memory usage by ~400 MB; message loss on `--max-turns` limit hits fixed; Windows worktree deletion no longer removes files outside the target directory; PR linking now captures PRs created in Bash calls exceeding the 30K inline limit. Background task notifications explicitly state that no human input has occurred. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Reflect usage dashboard** — Beta feature launched July 9, 2026; accessible via Settings > Reflect on the web and desktop app. Shows topics discussed, usage patterns, and task types over selectable 1, 3, 6, or 12 month windows. Includes mindful AI prompts ("What's one thing you'd keep doing yourself?"), quiet hours, and break reminders via Settings > Time and focus. Available to Free, Pro, and Max users with memory enabled; excludes incognito chats and health integration data. (Source: https://www.anthropic.com/news/reflect-with-claude)
- **Claude Code v2.1.206** — Released July 10, 2026. Adds directory path suggestions to `/cd` (matching `/add-dir` behaviour); a `/doctor` check that flags redundant checked-in `CLAUDE.md` files; `/commit-push-pr` now auto-allows `git push` to configured push remotes; agents view gets full-width status column with classifier-written headlines. Fixes: MCP servers now honour per-server `request_timeout_ms`; OAuth MCP tokens no longer require re-auth after refresh; expired login no longer shows misleading model error. `Ctrl+X` in agents view now permanently removes completed sessions. (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Code v2.1.207** — Released July 11, 2026. Auto mode now defaults to enabled on Bedrock, Vertex AI, and Foundry without the `CLAUDE_CODE_ENABLE_AUTO_MODE` opt-in flag; disable via `disableAutoMode` in settings. Fixed terminal freezing and keystroke lag when streaming responses containing very long lists, tables, paragraphs, or code blocks. Fixed auto-updater overwriting custom launcher scripts at `~/.local/bin/claude`. Fixed remote managed settings being permanently recorded as consented without showing the security consent dialog. Fixed spurious prompt-injection warnings triggered by benign system-generated conversation updates. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Desktop for Linux (beta)** — Claude Desktop is now available in beta for Ubuntu 22.04+ and Debian 12+ on x86_64 and arm64, providing the same Chat, Cowork, and Claude Code experience as macOS and Windows (parallel sessions, visual diff review, integrated terminal and editor, live app preview). Install via Anthropic's apt repository for automatic updates. Computer Use and voice dictation are not yet available on Linux. Launched alongside Claude Sonnet 5 in Week 27 (June 29, 2026). (Source: https://code.claude.com/docs/en/desktop-linux)
- **In-app browser in Claude Code Desktop** — Claude Code on desktop now includes a built-in browser, enabling Claude to pull up documentation, designs, or any website and interact with pages directly — the same way it interacts with local dev server previews. Launched Week 28 (July 6–10, 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **Auto mode warns before `rm -rf` on unresolved variables** — Since Week 28 (v2.1.205, July 8, 2026), auto mode asks for confirmation before executing `rm -rf` when the target variable cannot be resolved from session context, preventing accidental mass deletions when shell variable names are undefined. Complements the existing auto mode blocks on destructive git and infrastructure commands. (Source: https://code.claude.com/docs/en/whats-new)
- **Claude Code v2.1.208** — Released July 14, 2026. New features: screen reader mode (`claude --ax-screen-reader`, `CLAUDE_AX_SCREEN_READER=1` env var, or `"axScreenReader": true` in settings) renders the TUI as plain text for screen reader compatibility; `vimInsertModeRemaps` setting maps two-key vim insert-mode sequences (e.g. `jj` → Escape); `CLAUDE_CODE_PROCESS_WRAPPER` routes every self-spawned Claude Code process through a corporate launcher executable. Performance: session transcript size reduced up to 79× in edit-heavy sessions by pruning superseded file-history backups; per-tool-call CPU overhead reduced up to 7× when many MCP tools are present; multiple memory leaks fixed (MCP stdio server stderr capped at 64 MB, LSP documents capped to LRU-50, async hook output, tool-result payload accumulation); per-turn permission rule matching now compiled once and cached, eliminating multi-second slowdowns with many deny/ask rules. Also fixed: fast mode not restoring after switching back to a compatible model; background agent replies lost on delivery failure now re-delivered on restart; context window briefly resetting to 200k after auto-updates. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.209** — Released July 14, 2026. Single fix: `/model` picker and other dialogs were incorrectly blocked in `claude agents` background sessions due to an overly broad guard introduced in v2.1.208; now reverted. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.210** — Released July 14, 2026. New features: live elapsed-time counter on collapsed tool summary line so long-running calls visibly tick instead of looking stuck; startup warnings for deprecated `Write(path)`, `Glob(path)`, and `NotebookEdit(path)` permission rules (suggesting `Edit`/`Read` equivalents); screen reader now announces permission mode changes when cycling with Shift+Tab; memory writes that would push `MEMORY.md` over its read limit now produce an explicit error; agent view sessions stay visibly marked after navigation and background agent counts are colour-emphasised. Bug fixes: `isolation: 'worktree'` subagents no longer execute git commands against the main repo; `claude attach` failures during session transitions resolved; Grep pagination false "No matches found" fixed; plugin-provided MCP servers no longer torn down on mid-session resync; plan approvals no longer mislabelled as user edits; ghost frame overlap in alternate-screen mode eliminated. (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Dreaming (Managed Agents) adds Fable 5 and Sonnet 5 support** — As of July 10, 2026, the Claude Dreaming research preview supports Claude Fable 5 and Claude Sonnet 5 in addition to previously supported Opus-tier models. See the Managed Agents dreams documentation for the full supported-models list. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Claude Code v2.1.212** — Released July 17, 2026. `/fork` now duplicates the current conversation into a background session while the main session continues; `/subtask` replaces the previous in-session subagent command. New `claude auto-mode reset` CLI command restores default auto-mode configuration. Session-wide WebSearch limits (default 200 calls) and subagent spawn caps (default 200) prevent runaway agentic loops. MCP tool calls running over 2 minutes are automatically backgrounded to keep the session responsive. `/resume` now shows a picker including previously deleted sessions. Security fixes: auto mode blocks transcript tampering (complementing v2.1.205's file-write guard); worktree creation vulnerability involving symlinks that could write files outside the repository patched. Plan mode no longer auto-executes file-modifying Bash commands without a permission prompt. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Code v2.1.211** — Released July 15-16, 2026. New `--forward-subagent-text` flag and `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT` env var include subagent text and thinking in stream-json output. Bug fixes: parallel logout-on-sleep fixed; plugin MCP servers reconnect after idle; Bedrock/Vertex prompt-caching regression fixed; file upload validation hardened (DOS device suffixes, trailing dots, hard links); subagents no longer revert to parent model on resume; `/loop` no longer hides session from `/resume`; `/clear` now correctly resets session cost counter; background agents no longer auto-respawn after user kill; integer env vars now accept scientific notation (e.g. `1e6`). (Source: https://code.claude.com/docs/en/changelog)
- **Claude memory redesigned as individual categorized entries** — As of July 10, 2026, Claude's memory works as a set of individual, categorized entries that Claude reads and updates in real time during conversations, replacing the previous daily memory summary. The Memory panel lists all entries grouped by category; update entries directly from chat by telling Claude what to remember. Available on Free, Pro, and Max plans with memory enabled. (Source: https://support.claude.com/en/articles/12138966-release-notes)
- **Claude Code v2.1.215** — Released July 19, 2026. Claude no longer runs the `/verify` and `/code-review` skills automatically; invoke them explicitly with `/verify` or `/code-review` when you want them. (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Code v2.1.216** — Released July 20, 2026. New `sandbox.filesystem.disabled` setting allows skipping filesystem isolation for specific sandboxed commands. Also fixes: message normalization slowdown in long sessions; auto mode OAuth token expiration issues; Claude Code on web re-asking questions after idle periods; background agent session restoration failures. (Source: https://github.com/anthropics/claude-code/releases)
- **Claude Code v2.1.217** — Released July 21, 2026. Adds emoji shortcode autocomplete in prompts, clearer transcript write warnings, and tighter controls for subagents, budgets, and background sessions. Fixes memory leaks in MCP tool outputs, Windows auto-update failures, and background session isolation issues. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Artifacts call viewer MCP connectors (Week 29)** — Published artifacts can now pull live data and take actions through each viewer's own MCP connectors at view time, enabling live-data dashboards and interactive pages rather than static snapshots. Also added: public sharing links, editor roles for collaborative artifact editing on Team and Enterprise, and artifacts created directly from Claude Tag channel sessions. (v2.1.207–v2.1.212, Week 29, July 13–17, 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **Claude Code v2.1.214** — Released July 18, 2026. Key new feature: the **EndConversation tool** lets Claude end sessions with highly abusive users or jailbreak attempts (as on claude.ai since 2025). Security: extensive permission check fixes — single-segment `dir/**` allow rules now only match `<cwd>/dir`; PowerShell 5.1 permission-check bypass fixed; Bash checks fail closed on file-descriptor redirects; commands over 10,000 characters always prompt; zsh variable subscripts in `[[ ]]` now prompt; `help`/`man` with unsafe options no longer auto-approved; `docker` commands with daemon-redirect flags now require permission. New: periodic progress heartbeat for silent long-running tool calls; ISO `modified` timestamp in memory file frontmatter; `message.uuid`, `client_request_id`, `tool_source` attributes in OTel events; `CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH` to tune OTel truncation. Bug fixes: Bash pkill crash when pattern matches CLI; unbounded memory growth from oversized `--settings` files; Windows PowerShell streaming, Unicode, and `where.exe`/`fc.exe` fixes; background session and daemon reliability. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Cowork "Record a Skill"** — Launched July 21, 2026 on Pro, Max, and Team plans (phased rollout). Screen-record yourself doing a task once; Claude analyzes the recording and converts it into a reusable, rerunnable skill without any prompt engineering. Extracts semantic intent rather than recording click coordinates, making automations more resilient to UI layout changes than traditional RPA tools. Claude suggests API-based connectors when available for more reliable automation. (Source: https://www.explainx.ai/blog/claude-cowork-record-a-skill-screen-recording-july-2026)
- **Claude Code v2.1.218** — Released July 22, 2026. `/code-review` now runs as a background subagent, keeping review work out of the main conversation and preserving stacked slash commands as its review target. Screen-reader announcements added for word and line deletions (Option+Delete, Ctrl+W, Cmd+Backspace, Ctrl+U, Ctrl+K) in `--ax-screen-reader` mode. Fixed Windows path corruption where `\u`-prefixed segments like `C:\Users\unicorn` were being mangled into CJK characters in tool inputs. Left arrow key after editing now asks for confirmation before discarding conversation; Esc in agent view returns to the backgrounded conversation. `claude mcp list` and `/mcp` now show HTTP status/error text; hidden whitespace in MCP config values triggers a warning. Skills with `context: fork` now run in background by default. Subagent concurrency cap defaults to 20. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Voice Mode expanded to Opus and Sonnet models** — Launched July 23, 2026; voice mode now lets users choose between Opus, Sonnet, and Haiku models (previously Haiku-only). Defaults to the last model used in text chat at its fastest available version. Paid subscribers can switch models mid-conversation. Adds cross-app voice automation with Gmail, Google Calendar, Slack, Canva, and Notion. Expanded multilingual support to include English, French, German, Hindi, Indonesian, Italian, Japanese, Korean, Portuguese, and Spanish. Free users are limited to Haiku and one connected app; paid plans unlock all models and multi-app automation. (Source: https://techcrunch.com/2026/07/23/anthropic-updates-claude-voice-mode-with-more-capable-models/)
- **Claude Opus 5 launched** — Released July 24, 2026; described as "a thoughtful and proactive model that comes close to the frontier intelligence of Claude Fable 5 at half the price." Becomes the default model in Claude Code v2.1.219 with a 1M token context window. Priced at $5/$25 per MTok, positioning it between Fable 5 ($10/$50) and Opus 4.8 in cost. (Source: https://releasebot.io/updates/anthropic/claude)
- **Claude Opus 5 benchmark results** — Frontier-Bench v0.1: 43.3% agentic coding (doubles Opus 4.8); ARC-AGI-3: 30.2% (20× improvement over Opus 4.8, 3× the next-best model); OSWorld 2.0: 70.57% (surpasses Fable 5 at one-third the cost); CursorBench 3.2: within 0.5% of Fable 5 at half the cost; Zapier AutomationBench: 26.0% (~1.5× next-best); life sciences gains of +10.2 pp on organic chemistry and +7.7 pp on protein tasks. (Source: https://www.anthropic.com/news/claude-opus-5)
- **Opus 5: disabling thinking restricted at high-effort levels** — On Claude Opus 5, `thinking: {"type": "disabled"}` at effort `xhigh` or `max` returns a 400 error; disabling thinking is only allowed at effort `high` or below. Breaking change from Opus 4.8, where disabling thinking worked at any effort level. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **`workflowSizeGuideline` configurable setting** — New settings key to configure the Dynamic workflow size guideline in Claude Code, with a medium-size default targeting fewer than 15 agents per workflow (v2.1.219, July 24, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Subagent spawn depth default raised to 3** — In v2.1.219 (July 24, 2026), subagents can now spawn nested subagents up to depth 3 by default (previously 1); set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` to restore the old limit. (Source: https://code.claude.com/docs/en/changelog)
- **Fast mode for Claude Opus 5** — Fast mode is available for Claude Opus 5 at approximately 2.5× the standard output speed, priced at $10/$50 per million input/output tokens (double the standard Opus 5 rate of $5/$25). Became available when Opus 4.7 fast mode was removed on July 24, 2026; Opus 4.8 fast mode also continues. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Dreaming (Managed Agents) adds Opus 5 support** — As of August 1, 2026, the Claude Dreaming research preview supports Claude Opus 5 in addition to previously supported Fable 5, Sonnet 5, and Opus 4.8. See the Managed Agents dreams documentation for the full supported-models list. (Source: https://platform.claude.com/docs/en/release-notes/overview)
- **Claude Code v2.1.221** — Released August 4, 2026. New features: Focus view — a VS Code chat-menu toggle (Ctrl+Alt+F or "Claude Code: Toggle Focus view") that hides tool activity behind an expandable per-turn summary with a live running-tool indicator; sandbox credential masking (`mode: "mask"` for Linux/WSL) where sandboxed commands read a sentinel copy while the proxy substitutes the real value on egress; plugin name validation warnings in `claude plugin validate`. Key improvements: auto mode permission checks for parallel tool calls are now cache-efficient, cutting prompt-cache costs; Stats panel shows cache token breakdown by input, output, cache read, and cache write; background sessions commit, push, and follow CLAUDE.md git instructions; `/plugin install` refreshes stale marketplace catalog before reporting not found; plugins activate immediately when safe without needing `/reload-plugins`; `/fork` now creates a new worktree instead of working in the original session's checkout. Security fixes: Bash permission-check bypass in zsh `[[ ]]` regex conditionals; PowerShell permission checks with quote-containing paths; WebSearch 400 errors at xhigh/max effort with thinking disabled; sandboxed large upload TLS errors through sandbox proxy. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.222** — Released August 5, 2026. Security: fixed worktree-isolated sessions and subagents being able to run destructive git commands against the main checkout; fixed PreToolUse auto-allow hooks bypassing tool restrictions in background agent tasks; `SendMessage` calls now evaluated by the permission classifier before dispatch. Removes the ultraplan feature. Fixes: startup connectivity check hanging behind HTTPS proxy; "Connection closed mid-response" errors on completed responses; stream idle timeout firing on custom gateway endpoints despite keep-alive pings; `/usage` overattributing usage to MCP servers; sessions not linking to pull requests created after a branch push via GitHub REST API; org-restricted model aliases dropping to parent model instead of stepping down correctly; `SendMessage` rejecting long summaries (now truncates). Also improves `/diff` to use raw git blob content, ignoring workspace diff drivers. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Code v2.1.224** — Released August 7, 2026. New features: `claude self-hosted-runner` command turns your own machines or containers into environments where Claude Code web, mobile, and desktop sessions can run (Team and Enterprise plans); archive plugin source installs plugins from HTTPS-hosted zip files without git or npm, with optional SHA-256 pinning; cross-session messaging lets Claude Code sessions discover and message each other via `SendMessage` and `ListAgents` tools (macOS and Linux); sandbox credential masking gains `extract`/`onExtractNoMatch` for structured env values, `decode: "jwt"` with `maskClaims` for JWT-aware masking, and `awsPairs`/`sigv4` for AWS SigV4 re-signing; new `crossSessionInbound` and `dialogExpiry` settings manage inter-session message permissions; `ANTHROPIC_BEDROCK_REGION_PREFIX` env var enables Bedrock cross-region inference profile preference; 200-subagent-per-session spawn cap removed. Improvements: fullscreen mode keeps full pre-compaction history in scrollback; Remote Control now shows compaction progress, persistent connection-failure indicators, and archives stale server sessions. Bug fixes: long project paths resolving to wrong session directories; `SendMessage` falsely reporting success on delivery failure; sandbox filesystem deny entries with trailing slashes bypassable on Linux/macOS. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.223** — Released August 6, 2026. New: owner wildcard entries (`"owner/*"`) in `strictKnownMarketplaces` and `blockedMarketplaces` managed settings to allow/block all repos under a GitHub org; `/teleport` hint in cloud sessions showing `claude --teleport <session id>` to continue locally; `/review` is now an alias of `/code-review` (use `/code-review ultra` for deep cloud review); `CLAUDE_CODE_DISABLE_1M_CONTEXT` now applies auto-compaction to all 1M-window models to hold them at 200K (previously only a fixed list). Security: fixed Bash permission bypass where crafted commands could hide parts of themselves from permission checks; fixed permission prompts bypassed by tabs or invisible Unicode padding; fixed workflow scripts using dynamic `import()` to escape the workflow sandbox; fixed agent `bypassPermissions` mode ignoring the org-level bypass-permissions disable policy. Bug fixes: resuming sessions after `/cd` returning empty; gateway model discovery for provider-prefixed IDs; `modelOverrides` ignoring non-Anthropic keys; sandbox commands failing on Linux when `denyWrite` covers working directory; forked background agents stuck "already resuming"; `/code-review` reusing stale effort level from last invocation. (Source: https://code.claude.com/docs/en/changelog)
- **Enterprise skill and plugin security scanning** — Claude Enterprise organizations can enable automated security scanning for third-party skills and plugins. The system checks uploads and edits for malicious content before the extension becomes available to org members, helping prevent supply-chain attacks via untrusted skills. Beta feature, launched August 6, 2026. (Source: https://releasebot.io/updates/anthropic/claude)
- **Claude Code v2.1.225** — Released August 8, 2026. New features: gateway spend-limit support in the usage warning (the limit-reached message now names the cap, its reset time, and the operator's message); workspace trust prompt added to `claude agents` for untrusted directories, matching the behaviour of the `claude` command; Remote Control now shows photos attached from the Claude mobile app directly to Claude; `SendMessage` can now initiate a conversation with Remote Control sessions on other machines by name. Key fixes: transient 401 replacing a long-lived `CLAUDE_CODE_OAUTH_TOKEN` breaking headless sessions on restart; MCP OAuth servers on macOS intermittently failing with a burst of 401 errors after a keychain read timed out; auto mode incorrectly counting a safety-filter refusal of its own permission check toward the consecutive-block limit; cross-session messages staying parked without notice in headless sessions. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.227** — Released August 10, 2026. Key fixes and improvements: feature flags no longer evaluated without the user's subscription tier when a session starts with an expired login token; Bash commands no longer fail under `claude-code-action` with `allowed_non_write_users` on GitHub-hosted runners; `/tui` no longer resurfaces conversations rewound before their first message; slash-command menu improved (blue highlights only the selected row, matched characters bolded, emoji/accented names render correctly); fewer event-loop stalls on file-not-found suggestions and at-mention size checks. (Source: https://code.claude.com/docs/en/changelog)
- **Auto mode becomes default for all Pro/Max/Team accounts on August 14** — Anthropic announced that auto mode will be enabled by default for interactive Pro, Max, and Team Claude Code sessions starting August 14, 2026. Previously, manual approval at each step was the default. In auto mode, Claude proceeds without prompts unless an action is irreversible, destructive, or targets outside your environment. Internal testing with 1,053 paid testers found automated safety measures caught 89% of harmful actions vs. 13.6% for human review, and that users approved 97% of prompts anyway. New safeguards being added alongside: prompt injection screening and customisable hard deny rules. (Source: https://techcrunch.com/2026/08/09/anthropic-is-turning-claude-codes-auto-mode-on-by-default/)
- **Claude Code v2.1.228** — Released August 11, 2026. Key improvements: synced skills from claude.ai hardened (no longer shadow local commands or MCP prompts; bodies cannot run `!` commands or expand `@` files); cross-session messages now display sender and body inline instead of collapsed; Vertex AI credential failures now surface within seconds rather than retrying for minutes; compaction progress now shows retry countdown and stall hints. Changed: Write tool now lets newer models overwrite unread files without a prior Read (matching Edit tool rules); removed outdated auto-mode cost note from first-use notices for Pro, Max, and Team. Bug fixes: interactive session redraw halt after rare layout error; Windows git detection failure when launched from parent folder; `/tui` reverting to earlier model after `/model` change; cross-session inbox missing after fresh install; Remote Control `/resume` leaking conversation history into new session; session cleanup deleting project memory folder contents. (Source: https://code.claude.com/docs/en/changelog)
- **iOS Simulator pane in Claude Code Desktop** — Claude Code Desktop on macOS gets a built-in iOS Simulator pane (public beta on Pro, Max, and Team plans). When Claude builds or launches an app in the simulator, the pane opens alongside the conversation and streams the device screen live, letting you watch Claude tap through the app to verify changes or drive the device yourself. Launched Week 30 (July 20–24, 2026). (Source: https://9to5mac.com/2026/07/21/claude-code-brings-live-ios-app-testing-into-its-mac-app/)
- **Claude Security plugin (multi-agent vulnerability scan)** — A dedicated security plugin that runs a multi-agent scan of your codebase from within a Claude Code session: agents map the architecture, build a threat model, hunt for vulnerabilities, independently review every finding, and produce a report. Distinct from the bundled security-guidance plugin (which reviews changes inline). You pick which findings to turn into patches. Available as a marketplace plugin. Launched Week 30 (July 20–24, 2026). (Source: https://code.claude.com/docs/en/whats-new)
- **Claude Code v2.1.229** — Released August 12, 2026. New features: server-supplied hooks for self-hosted runner sessions; SSE keepalive pings on gateway streaming responses during long thinking pauses (prevents Vertex/Bedrock idle-timeout disconnects); plugin marketplace `command` sources allow local commands to print plugin directories, re-resolved each session; `ListAgents` now marks disconnected Remote Control sessions as `offline` and cloud sessions as `cloud`; improved workflow agent staggering for prefix cache reuse. Key fixes: streaming response corruption where long responses partly disappeared and were printed twice; tool call crashes with non-string `glob`/`file_path`/`command` fields; terminal RangeError crashes in very narrow windows; Windows extended-length and UNC path crashes; multi-second IDE stalls with thousands of diagnostics; GitHub workflow not posting code reviews on PRs; dynamic workflows using host core count instead of container limit. (Source: https://code.claude.com/docs/en/changelog)
- **Subagent forking on by default + cross-session @ mentions (v2.1.232)** — Released August 13, 2026. Subagents using `subagent_type: "fork"` now inherit the full conversation and prompt cache by default; non-teammate agents in interactive sessions run in the background by default. Type `@` in any prompt to mention another Claude Code session by name, and `SendMessage` delivers to exact name matches without a confirmation step. Fable 5 `/advisor` is re-offered for organisations with Fable access. Also: GitLab token families now redacted (`glrt-`, `gloas-`, and others); settings aliases `additionalMarketplaces` and `allowedMarketplaces` accepted as alternatives to `extraKnownMarketplaces` and `strictKnownMarketplaces`; PowerShell permission bypass via variable-writing parameters fixed; Windows Git Bash Cygwin-style symlink bypass fixed. (Source: https://code.claude.com/docs/en/changelog)
- **Claude text watermarking (SynthID-Text)** — All Claude models released on or after August 2, 2026 embed imperceptible watermarks in generated text using the SynthID-Text method (developed by Google DeepMind). The watermark statistically biases word choices using a secret cryptographic key, without affecting meaning or quality. Covers the Claude Platform API, claude.ai, Claude Code, Cowork, Claude Tag, and models on AWS, Google Cloud, and Microsoft Foundry. A watermark detection API is forthcoming; older models gain support over coming months. Launched to comply with the EU AI Act, applied globally. (Source: https://www.anthropic.com/news/claude-text-watermark)
- **SynthID-Text watermark technical details clarified** — Anthropic's August 15, 2026 technical explainer confirms: (1) light editing doesn't remove watermarks, but a complete word-for-word rewrite will (though fully rewritten text may no longer qualify as AI-generated); (2) code generation receives minimal watermarking since models have little flexibility in generating functionally correct code — only arbitrary choices like comment wording are watermarked; (3) heavily human-edited text has little for the watermark to attach to, making detection unreliable on such content. A detection API is forthcoming. (Source: https://techcrunch.com/2026/08/15/anthropic-shares-more-details-about-how-claudes-new-watermarks-will-work/)
- **Claude in Chrome upgraded to full Cowork session** — The Claude Chrome side panel, previously a standalone chat interface, became a full Claude Cowork session (August 12, 2026). Conversations now save to history and sync across desktop, web, and mobile; skills and connectors work in-browser; tasks continue across devices. Safety checks run before consequential actions to mitigate prompt injection risks. Available on Max and Team plans; rolling out to Pro. (Source: https://claude.com/blog/cowork-chrome-side-panel)
- **Claude Tag proactive responses with full channel context** — Claude Tag's Slack integration now analyzes full channel history and memory to determine when to respond proactively, not just when @-mentioned (August 13, 2026). Accuracy of proactive participation improved by ~30%; Claude can reply inline, start threaded work, route to existing workstreams, or stay silent. Customisable with plain-language instructions like "Never respond unless tagged." No additional cost. (Source: https://releasebot.io/updates/anthropic/claude)
- **Claude Code v2.1.234** — Released August 17, 2026. Adds `CLAUDE_CODE_PROJECT_DIR_NAME` env var for custom per-project transcript directory names; a GitLab merge request badge to the footer and statusline (`MR !N` with draft/pending/green states); `CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS` to tune the WebFetch cache TTL (default: 15 minutes); and automatic session continuation when a claude.ai usage limit resets (configurable in `/config`). Also includes tighter permission and security controls and stronger worktree isolation blocking destructive git commands against the main checkout. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.235** — Released August 18, 2026. Adds an optional `spellcheck` setting that underlines misspelled words in the prompt input using the system-installed `aspell`, `hunspell`, or `ispell` spell-checker. Also fixes whole-prompt-cache invalidation triggered by a language server disconnect, fixes Shift+Tab in a permission prompt bypassing field closure, and resolves several UI rendering issues including list alignment at depth 3+ and prompt-highlight misalignment in multi-line prompts. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.233: GitLab worktree, todo tool removal, memory limits** — `--worktree` and `claude agents` now support GitLab MR URLs (displayed as `!N`). Todo/task-tracking tools removed for Opus 4.8+, Sonnet 5+, Fable 5+, and Mythos 5+ (restore with `CLAUDE_CODE_ENABLE_TODO_TOOLS=1`). New `CLAUDE_CODE_TOOL_MEMORY_LIMIT` env var caps Bash tool memory on Linux via cgroup. `forward_user_identity` apps gateway setting enables per-user spend attribution. Fixed idle sessions pegging a CPU core at 100% with sandboxing enabled (v2.1.233, August 14, 2026). (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.234: Auto-continue on usage limits, GitLab MR badges, NTLM security fix** — Released August 17, 2026. Claude Code now automatically continues when usage limits reset instead of requiring manual restart. GitLab merge request badges appear in the footer and statusline. New `CLAUDE_CODE_PROJECT_DIR_NAME` env var controls per-project transcript directory names. `selection:clear` keybinding action added. Security: hardened against NTLM credential-leak vector; Windows NT-namespace path rejection added. Also fixes auto mode in long sessions with repeated re-checking, session-scoped permission answers being dropped, and markdown rendering slowdowns. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.235: Optional spellcheck for prompt input** — Released August 18, 2026. Adds an optional `spellcheck` setting that uses `aspell`, `hunspell`, or `ispell` to check spelling in the prompt input field. Fixes prompt cache invalidation for language server disconnections, nested markdown list alignment at depth 3+, and Shift+Tab behavior in permission prompts. Improves memory and CPU usage for long-running cloud sessions such as `/ultrareview`. Various Vim mode and dialog improvements. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.236: ANTHROPIC_DEFAULT_MODEL env var, notify_when_idle cross-session** — Released August 19, 2026. New `ANTHROPIC_DEFAULT_MODEL` environment variable sets the default model for all new sessions without needing to configure it per-project. `notify_when_idle` added to cross-session `SendMessage` for opt-in notifications when another Claude Code session goes idle. Sandbox improvements: wildcard read-deny rules on macOS now correctly take precedence inside allowed regions. Improves auto mode with better git status checking; Remote Control marks sessions offline within seconds when CLI exits. Also fixes clipboard copy, background sessions, local MCP logs after directory removal, `/model` picker rendering, and fullscreen renderer fallback. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.237: Built-in "Concise" output style, prompt caching fix for gateways** — Released August 20, 2026. Adds a built-in "Concise" output style where Claude leads with results and skips preamble while still completing work thoroughly — useful for scripted or CI contexts. Fixes prompt caching for sessions using an LLM gateway or custom base URL, which had been incorrectly invalidating the cache prefix. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.239** — Released August 22, 2026. Cost estimates (`/cost`, status line, `--max-budget-usd`) now include the 1.1× US-only-inference premium for data-residency workspaces. Fullscreen renderer offer added to Bedrock, Vertex, Foundry, and other previously excluded setups. New `/claude-api upgrade` skill migrates Python projects from `anthropic` 0.x to 1.x. Cloud sessions: plugins synced from claude.ai show as `name@synced` and work with `claude plugin enable/disable`. Alpine/musl builds: native image paste, clipboard, and audio-capture add-ons now load. Many bug fixes: Bedrock streaming behind proxies, JetBrains IDE terminal 5-second pauses, WebFetch cache expiry, race conditions in session transitions, MCP server resilience, and dozens more. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.243** — Released August 25, 2026. New features: `modelPicker` setting lets you curate the `/model` picker with an ordered, labelled list of models; Loops breakdown in `/usage` shows per-loop run count, total tokens, tokens per run, and last run; `promptCacheTtl` and `subagentPromptCacheTtl` settings control prompt cache duration; `modelPricing` managed setting lets organisations configure contracted per-model rates; keyless sign-in via Anthropic Console (under `/login` → "Sign in with your Console account"); remote MCP servers in non-interactive (`-p`) and SDK sessions now auto-reconnect after dropped connections. Performance: native Linux binary zstd-compressed to ~75 MB (down from 340 MB); per-session memory usage reduced 40–70 MB via on-demand code loading. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.238: keybindingFlavor, Remote Control resilience, memory leak fix** — Released August 21, 2026. New `keybindingFlavor` setting: set to `"readline"` for Ctrl+W to delete back to previous whitespace (Bash-style), default is `"classic"`. Plugin marketplace `headersHelper` runs commands that mint short-lived HTTP headers for catalog and archive fetches. Memory leak fixed: subagent tool results now released after they leave the display window in long sessions. Fixed custom output styles drifting to default mid-session. Extensive Remote Control resilience improvements including cross-session messaging, session crash recovery, model-pick sync from phone/web, and reconnect handling. (Source: https://code.claude.com/docs/en/changelog)
- **Claude memory expanded across Chat and Cowork with editable topics** — As of August 25, 2026, Claude's memory works seamlessly across Chat and Claude Cowork in the cloud. Users can view everything Claude remembers topic by topic and edit or delete any entry. New sensitive-topics controls let users choose whether Claude remembers health or belief-related information. Enabled by default for Free, Pro, and Max plans. (Source: https://releasebot.io/updates/anthropic/claude)
- **Claude Code v2.1.245** — Released August 25, 2026. Fixes a critical startup crash on Linux distributions shipping glibc 2.44 (Arch Linux, CachyOS, Fedora Rawhide). (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.246** — Released August 25–26, 2026. Adds an Auto mode tab to `/permissions` for viewing and editing auto mode classifier rules; adds a turn completion timestamp to end-of-turn lines. Key fixes: severe transcript slowdown with very long single lines (e.g. base64 strings); erratic fullscreen scrolling; background sessions failing to open with "EACCES" errors; MCP tool arguments being sent as JSON strings instead of real types; auto mode safety-check deadline scaled on very large sessions; telemetry requests incorrectly carrying third-party gateway API keys. Also improves `/cd` so new directory settings, hooks, MCP servers, skills, and agents take effect immediately without restart. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.247** — Released August 27, 2026. New features: `SendFeedback` tool lets Claude draft error reports when issues occur during a session; `/claude-api cost-optimize` subcommand profiles API spending across caching, token usage, batching, and model selection to surface optimization opportunities. Also improves auto-compact handling, permission prompts, and keyboard support; fixes history search, Remote Control connectivity, setup flows, and background session reliability. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Cowork autonomous built-in browser** — Claude Cowork's desktop app gained a built-in sandboxed browser on August 26, 2026 for autonomous web navigation. Claude uses this private environment to research and act on the web without accessing the user's personal browser history, tabs, bookmarks, or passwords. Complements the Claude Code desktop in-app browser (launched Week 28, July 2026) but is distinct in being a fully sandboxed context that Claude controls autonomously for Cowork tasks. (Source: https://releasebot.io/updates/anthropic)
- **Claude Code v2.1.248** — Released August 28, 2026. New "restricted mode" removes built-in command and code tools for high-security or minimal-surface deployments; cross-session messaging improvements; a usage credits command for Enterprise to query spend from the CLI. Key prompt caching fix: OAuth token refreshes were causing one cache miss per hour in long sessions, now resolved. Also fixes Desktop sessions disappearing from the agents view after 30 days and intermittent Remote Control message delivery failures. (Source: https://releasebot.io/updates/anthropic/claude-code)
- **Claude Code v2.1.252** — Released August 31, 2026. Bug-fix release: fixes Bash commands failing with "task output swap refused" on some Macs; "always allow" not saving in projects without `.claude/settings.local.json`; Remote Control sessions stalling for minutes after tool completion during degraded claude.ai connections; background task notifications with large failure output exceeding API request size limits. (Source: https://code.claude.com/docs/en/changelog)
- **Claude Code v2.1.251** — Released August 28, 2026. New `PreModelSwitch` and `PostModelSwitch` hook events let hooks block, confirm, or annotate model switches. Live streaming of foreground subagent tool calls and results to Remote Control clients. Spend limit bar added to `/usage` and `rate_limits.spend_limit` status line field; per-session prompt-cache stats line in `/cost` showing hit ratio, misses, tokens re-cached, and warm/cold state. Adds `attach`, `logs`, `stop`, `respawn`, and `rm` to `claude --help`. Default model for seat-based Enterprise subscriptions changed to Opus 5. Native binary ~5 MB smaller. Security: fixes path traversal via symlinks in file tools (Read/Write/Edit), plugin commands pointing outside their directory, Grep/Glob not applying deny rules to symlinked search paths, Workflow tool reading `scriptPath` outside approved location pre-permission-check, and project settings bypassing OTLP collector pinning. (Source: https://code.claude.com/docs/en/changelog)
